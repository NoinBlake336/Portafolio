import styled from "styled-components";

export const TextStyled = styled.p`
    font-size: 1rem;
    font-weight: 400;
    color: #fff;
    span {
        font-weight: 500;
        color: var(--text-accent);
    }
`;